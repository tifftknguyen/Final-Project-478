
# These owners will be the default owners for everything in
# the repo. Unless a later match takes precedence,
# @tifftknguyen and @gissyhernandez will be requested for
# review when someone opens a pull request.
*       @tifftknguyen @gissyhernandez

# You can also use email addresses if you prefer. They'll be
# used to look up users just like we do for commit author
# emails.
*.go tifftknguyen@gmail.com
*.go gisellehernandez907@gmail.com

# In this example, @tifftknguyen owns any files in the build/logs
# directory at the root of the repository and any of its
# subdirectories.
/build/logs/ @tifftknguyen

# In this example, @tifftknguyen owns any file in an apps directory
# anywhere in your repository.
apps/ @tifftknguyen

# In this example, @tifftknguyen owns any file in the `/docs`
# directory in the root of your repository and any of its
# subdirectories.
/docs/ @tifftknguyen

# In this example, @tifftknguyen owns any file in a `/logs` directory such as
# Any changes in a `/logs` directory will require approval from @tifftknguyen.
**/logs @tifftknguyen

# In this example, @tifftknguyen owns any file in the `/apps`
# directory in the root of your repository except for the `/apps/github`
# subdirectory, as its owners are left empty. Without an owner, changes
# to `apps/github` can be made with the approval of any user who has
# write access to the repository.
/apps/ @tifftknguyen
/apps/github

# In this example, @octocat owns any file in the `/apps`
# directory in the root of your repository except for the `/apps/github`
# subdirectory, as this subdirectory has its own owner @doctocat
/apps/ @gissyhernandez
/apps/github @gissyhernandez